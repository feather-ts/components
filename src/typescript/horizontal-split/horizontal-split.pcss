horizontal-split {
    display: flex;
    flex-direction: row;
    align-items: stretch;
    touch-action: none;

    > * {
        overflow-x: hidden;
    }

    > *:last-child {
        flex: auto;
    }

    .handle {
        flex: none;
        width: 5px;
        cursor: ew-resize;
        border-left: .5px solid gray;
        border-right: .5px solid gray;
        user-select: none;

        &:hover {
            background-color: rgba(0, 0, 0, .1);
        }
    }
}
